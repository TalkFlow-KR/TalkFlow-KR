import styled from "styled-components"
import { Box } from "./Article.styled";

export const Wrapper = styled(Box)`
  width: 28rem; 
  height: 83vh;
  display: flex;
  /* flex-direction: column; */
  margin-top: 2.4rem;
  flex-direction:column;
  /* padding: 0.2.4rem; */
  left: 0;
  position: fixed;
  top:10.4rem;
  border-radius:2rem;
  flex-shrink:1;
  box-shadow: 0.5rem -0.2rem 4rem 0.7rem rgba(0, 0, 0, 0.25);

  
  `